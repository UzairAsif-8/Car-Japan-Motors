# Loaded automatically by Vite during `vite build` (production).
# This guarantees the deployed frontend points at the live backend even if the
# Vercel dashboard env var is missing. A Vercel-dashboard VITE_API_BASE_URL,
# if set, takes precedence over this file.
VITE_API_BASE_URL=https://car-japan-motors.onrender.com
